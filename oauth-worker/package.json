{
  "name": "mcptest-oauth-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for handling OAuth 2.1 callbacks",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "keywords": ["oauth", "cloudflare-worker", "oauth2.1"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.0.6"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}