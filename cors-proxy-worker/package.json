{
  "name": "mcptest-cors-proxy",
  "version": "1.0.0",
  "description": "CORS proxy worker for MCP Test with authentication",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'No build step required for TypeScript Workers'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "keywords": ["cloudflare-worker", "cors", "proxy", "mcp"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.0.0"
  }
}