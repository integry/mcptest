# mcptest handoff — 2026-08-13

Resume the GitHub/Ultrafix review, validation, merge, and deployment workflow in `/var/www/mcptest`.

## Non-negotiable rules

- Work is free/no feature gating.
- Use the `AI` and `ultrafix` issue/PR workflow.
- Trust Ultrfix for implementation, but independently inspect and validate every exact PR head.
- Merge only when the latest exact-head full review score is strictly greater than 8 (9/10 or 10/10), all authoritative checks are green, the PR is mergeable, and independent exact-head validation passes.
- Generic `/review codex` reviews of large PR #273 receive truncated GitHub diffs and cap the score at 7. Use a checkout-based whole-branch audit instead.
- If uncertain, request a separate Codex review. Never merge based only on an optimistic automated score.
- Use `npx vite build`, not `npm run build`.
- `.git` may be read-only. Immutable GitHub tarballs in `/tmp` are the established validation method.
- Exact atomic squash merge:
  `gh api -X PUT repos/integry/mcptest/pulls/PR/merge -f merge_method=squash -f sha=EXACT_HEAD`
- Preserve support for stateless and stateful Streamable HTTP plus legacy HTTP+SSE.
- OAuth UX: autodiscover first; manual client credentials are last resort. Auth-required report cards must be calm prerequisites, not 0% error walls.

## Current master and merged work

- Current remote master/base: `c4fa9e8c5af5bdeb16a57897eb736649ae210901`.
- Already merged and production deployed:
  - #266, review 9.
  - #267, exact head `6021d13`, review 9, independently 625 tests + TS + Vite.
  - #268, exact head `2888883`, review 9, independently 723 tests + TS + Vite/schema/live preview.

## Critical path: PR #273 — hosted Slack/GitHub OAuth

- PR: https://github.com/integry/mcptest/pull/273
- Branch: `271/gpt-5.6-sol-p0-hosted-operator-oauth--20260812-0105-4ir`
- Current exact head: `3561a30bbf08f018c67b63f1547dfb831fec91ae`
- Base: `c4fa9e8c5af5bdeb16a57897eb736649ae210901`
- Mergeable: yes.
- Checks at handoff: all green:
  - Tests, TypeScript, and Wrangler dry run
  - Workers Builds: cors-proxy-worker
  - Workers Builds: mcptest-io

### Production Worker migration state

- The Durable Object bootstrap limitation was solved by an independently validated non-versioned deployment from exact PR head `f14d833`.
- Live Worker deployment/version after bootstrap: `399c7b36-39c4-4528-a769-2f679ddcb92e` (later PR builds may have deployed newer versions).
- Live Worker responded with CORS/proxy provenance correctly.
- `npx wrangler secret list --name cors-proxy-worker` returned `[]`.
- Therefore hosted Slack/GitHub OAuth will fail closed as `provider_not_configured` until operator secrets are supplied:
  - Slack client ID/secret/scopes
  - GitHub client ID/secret/scopes
  - token encryption key
- Never invent or extract these credentials. Ask the user when code PRs are complete.

### Exact-head validation already completed for `3561a30`

- Worker: 2 files, 33 tests passed.
- Worker TypeScript passed.
- GitHub Worker validation/dry run passed.
- Root: 46 files, 763 tests passed.
- Root TypeScript passed.
- Vite production build passed.
- Direct inspection confirmed:
  - exact Slack/GitHub target+issuer activation; Figma disabled;
  - provider tokens AES-GCM encrypted server-side;
  - opaque user/target-bound grants;
  - PKCE, state, replay, expiry, alarm cleanup;
  - 408/425/429/5xx refresh failures preserve grants;
  - only explicit invalid/revoked refresh credential errors delete grants.

### Full checkout audit result: two current blockers

A checkout-based audit inspected all 32 changed files/ranges against master and scored 7/10 with two blockers:

1. Hosted OAuth success is falsely reported as unauthenticated.
   - `src/utils/evaluation.ts`, authorization evidence call near prior line 1389 omits `selectedHostedGrant`.
   - A successful hosted-grant run can set `unauthenticatedTargetRequestSucceeded: true`, corrupting stored/shared auth readiness.
2. Target-origin 401/403 can bypass hosted-grant recovery.
   - Hosted branch of `connectForEvaluation` directly awaits the proxied connection.
   - It must wrap failure in `EvaluationConnectionError([makeRouteFailure('proxy', ...)])` so target-side Slack/GitHub 401/403 enters the authorization-required/stale-grant recovery path.
   - Preserve the existing special `HostedGrant invalid_token` proxy rejection recovery.

Direct local inspection confirmed both findings.

### Active ProPR queue at handoff

- A duplicate full checkout audit is currently running from comment `5276584385` / task shown in PR comments. It was requested before the first audit result appeared.
- A scoped fix request is queued behind it in comment `5276625604`:
  - include selected hosted grant in auth evidence;
  - wrap hosted-proxy failures into structured route failure evidence;
  - add regressions for hosted success provenance and target-origin 401/403 stale-grant recovery.
- Do not post more duplicate work yet. Poll comments/head. Once the fix starts/completes, pin the new SHA and validate it.

### Next steps for #273

1. Poll:
   `gh api repos/integry/mcptest/issues/273/comments --paginate --jq '.[-6:][] | [.created_at,.user.login,(.body|split("\n")[0:20]|join(" "))] | @tsv'`
2. When the fix produces a new head, download the immutable tarball and independently run:
   - Worker tests/typecheck/dry-run (the environment approval layer sometimes times out Wrangler; split stages if needed).
   - focused evaluation/OAuth/UI tests.
   - full root tests, TS, Vite build.
   - direct audit of both findings.
3. Wait for all three checks green.
4. Request another checkout-based whole-branch audit pinned to the new exact SHA; generic `/review codex` will likely be partial and score-cap at 7.
5. Merge only on full-scope 9+ and exact-head atomic revalidation.
6. After merge, production deployment should run. Confirm master, site HTTP 200, and Worker deployment.

## PR #276 — tool-selection evaluation suite

- PR: https://github.com/integry/mcptest/pull/276
- Current exact head: `f073db9303a292baf979096876b2af5d88af23ba`
- Branch: `249/gpt-5.6-sol-p1-tool-selection-evaluat-20260813-0239-5km`
- Mergeable: yes.
- Base: old master `c4fa9e8`.
- Current checks: web app green, Worker red because this branch predates #273's Worker binding/config changes after the production DO bootstrap.
- Independent validation at exact head:
  - focused eval/UI: 5 files, 72 tests.
  - full: 48 files, 795 tests.
  - TypeScript passed.
  - Vite build passed.
- Direct audit passed for latest fixes:
  - redaction now fails closed if scoring operands change and validates trial relationships;
  - token counts require non-negative safe integers;
  - per-trial costs require finite non-negative values;
  - prior dataset mutation/input schema/security fixes are present.
- Latest ordinary review before this head found F19/F20 and scored 6; those are fixed at `f073db9`.
- Do not request final review yet while Worker is red.

### Next steps for #276

1. After #273 merges, comment `/merge` to bring new master into #276 and let ProPR resolve conflicts.
2. Pin the resulting exact SHA; rerun independent full validation because merge resolution changes the head.
3. Require all checks green.
4. Request a fresh exact-head review (checkout-based if GitHub reports partial scope).
5. Merge only at full-scope 9+.

## PR #277 — scheduled monitoring/drift alerts

- PR: https://github.com/integry/mcptest/pull/277
- Current exact head: `cb926f4903527ce9b36dc49413fc6bd9c2020bc0`
- Branch: `250/gpt-5.6-sol-p1-scheduled-mcp-monitori-20260813-0339-bo3`
- Mergeable: yes.
- Base: old master `c4fa9e8`.
- Current checks: web app green, Worker red for the same pre-#273 config reason.
- Independent validation at exact head:
  - focused monitoring/evaluation/transport: 4 files, 98 tests.
  - full: 45 files, 748 tests.
  - TypeScript passed.
  - Vite build passed.
  - `npm run monitor -- --help` passed.
- Direct audit passed for:
  - inter-process filesystem lease;
  - atomic stale-lock takeover with identity revalidation/quarantine rename;
  - deterministic delayed-contender regression;
  - retention/alert artifact preservation;
  - CLI infrastructure exit classification;
  - stateful/stateless transport support.
- Latest ordinary review found F10-F12 at score 5. Ultrfix fixed them; independent audit found and then Ultrfix fixed a stale-takeover race at `cb926f4`.
- Do not request final review while Worker is red.

### Next steps for #277

Same as #276: after #273 merges, `/merge` master, pin new SHA, independently validate, require green checks, request full-scope review, merge only at 9+.

## Figma / OAuth operational context

- Figma endpoint behavior is not a normal DCR flow:
  - MCP endpoint 401 is expected.
  - protected-resource and authorization-server metadata are valid.
  - DCR returns 403 and requires provider approval/catalog registration.
- Issue #272 tracks external Figma approval. At last check it was open with no comments/update.
- Linear, Notion, and Atlassian worked in the browser; Figma/Slack/GitHub were the failing set.
- Figma hosted OAuth is deliberately disabled in #273.

## Deployment/infrastructure notes

- SSH target supplied by user: `root@65.109.86.240`; it previously timed out intermittently.
- Branch-preview deployment had shared remote-ref lock races; this was deployment infrastructure, not application code.
- GitHub/Cloudflare API calls sometimes stall; use small calls, terminate and retry once.
- Combined `npm run check` can be killed by the environment approval-review timeout during Wrangler even when tests are fine. Split into `npm test`, `npm run typecheck`, and dry-run/check; exact GitHub validation is authoritative when green.
- User pasted live Reddit session cookies earlier. Never use them; advise rotation/revocation when appropriate.
- Reddit research already covered 36 full threads plus 40+ snippets; roadmap tracked in #254.

## Useful status commands

```bash
gh pr view 273 --repo integry/mcptest --json headRefOid,mergeable,statusCheckRollup
gh pr view 276 --repo integry/mcptest --json headRefOid,mergeable,statusCheckRollup
gh pr view 277 --repo integry/mcptest --json headRefOid,mergeable,statusCheckRollup
gh api repos/integry/mcptest/issues/273/comments --paginate --jq '.[-6:][] | [.created_at,.user.login,(.body|split("\n")[0:20]|join(" "))] | @tsv'
```

## Immediate continuation prompt

Resume from this handoff. First poll PR #273 comments/head. Do not duplicate the currently running checkout audit or queued blocker fix. When the fix lands, pin the immutable SHA, validate it independently, wait for all checks, request a full checkout-based whole-PR audit, and merge only at 9+. Then merge master into #276 and #277 and continue their exact-head validation/review cycles.
